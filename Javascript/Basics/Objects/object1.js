// 1. Object.keys() — 🔑
// ➡️ Returns an array of property names (keys) of an object.

const person = {
  name: "Alice",
  age: 25,
  city: "Delhi",
};

const keys = Object.keys(person);
console.log(keys);
