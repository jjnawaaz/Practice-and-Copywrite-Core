/**
 * 2. Object.values() — 💎➡️ Returns an array of property values of an object.
 */

const person = {
  name: "Alice",
  age: 25,
  city: "Delhi",
};

const values = Object.values(person);

console.log(values);
