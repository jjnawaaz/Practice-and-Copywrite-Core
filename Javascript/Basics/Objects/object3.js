/**
 * 3. Object.entries() — 🎫➡️ Returns an array of [key, value] pairs.
 */

const person = {
  name: "Alice",
  age: 25,
  city: "Delhi",
};

const entries = Object.entries(person);
console.log(entries);
