const fs = require("fs");
const path = require("path");
const os = require("os");

console.log("OS Info: " + os.platform());
console.log("Directory name: " + __dirname);
console.log("File name: " + __filename);
